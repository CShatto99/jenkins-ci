#!/usr/bin/env groovy

pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo 'Building application on dev branch...'
			}
		}
		stage('Test') {
			steps {
				echo 'Testing application on dev branch...'
			}
		}
	}
}